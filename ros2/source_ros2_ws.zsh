#!/bin/zsh

# 1. make the script executable, if needed: $ chmod +x ./source_ros2_ws.zsh
# 2. run the script : $ ./source_ros2_ws.zsh to source RO2 and ros2_gym_pybullet_drones

source ~/ros2_foxy/ros2-osx/setup.zsh
source ./install/setup.zsh